<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <display-name>Ticket Tracker Application</display-name>
    <description>
        Comprehensive ticket tracking and workflow management system
        built with Java 8, Oracle 19c, and deployed on Tomcat 8.5
    </description>

    <!-- Application Context Parameters -->
    <context-param>
        <param-name>app.name</param-name>
        <param-value>Ticket Tracker</param-value>
    </context-param>

    <context-param>
        <param-name>app.version</param-name>
        <param-value>1.0.0</param-value>
    </context-param>

    <context-param>
        <param-name>uploadDirectory</param-name>
        <param-value>/opt/ticket-tracker/uploads</param-value>
    </context-param>

    <!-- Session Configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure> <!-- Set to true in production with HTTPS -->
        </cookie-config>
    </session-config>

    <!-- Welcome File List -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <!-- Error Pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/index.html</location>
    </error-page>

    <error-page>
        <error-code>500</error-code>
        <location>/error.html</location>
    </error-page>

    <!-- MIME Type Mappings -->
    <mime-mapping>
        <extension>json</extension>
        <mime-type>application/json</mime-type>
    </mime-mapping>

    <mime-mapping>
        <extension>js</extension>
        <mime-type>application/javascript</mime-type>
    </mime-mapping>

    <mime-mapping>
        <extension>css</extension>
        <mime-type>text/css</mime-type>
    </mime-mapping>

    <mime-mapping>
        <extension>woff2</extension>
        <mime-type>font/woff2</mime-type>
    </mime-mapping>

    <mime-mapping>
        <extension>svg</extension>
        <mime-type>image/svg+xml</mime-type>
    </mime-mapping>

    <!-- Servlet Registration -->
    <!-- All servlets use @WebServlet annotations for automatic registration -->
    <!--
         Registered servlets with @WebServlet annotation:
         - AuthServlet: /api/auth/*
         - TicketServlet: /api/tickets/*
         - UserManagementServlet: /api/users/*
         - ModuleServlet: /api/modules/*
         - WorkflowStepServlet: /api/workflow-steps/*
         - WorkflowCommentServlet: /api/workflow-comments/*
         - FileServlet: /api/files/*
         - FinanceApprovalServlet: /api/finance-approvals/*
         - DependencyServlet: /api/dependencies/*
         - FieldConfigServlet: /api/field-config/*
         - FieldValueServlet: /api/field-values/*
         - FileReferenceServlet: /api/file-references/*
         - FileReferenceTemplateServlet: /api/file-reference-templates/*
         - UserPreferencesServlet: /api/user-preferences/*
         - AuditServlet: /api/audit-logs/*
    -->

    <!-- Filter Registration Examples -->
    <!-- Uncomment and configure filters as needed -->

    <!--
    <filter>
        <filter-name>SessionFilter</filter-name>
        <filter-class>com.tickettracker.filter.SessionFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>SessionFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>CORSFilter</filter-name>
        <filter-class>com.tickettracker.filter.CORSFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>CORSFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>
    -->

    <!-- Listener Registration -->
    <!--
    <listener>
        <listener-class>com.tickettracker.listener.ApplicationStartupListener</listener-class>
    </listener>
    -->

</web-app>
